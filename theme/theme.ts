import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    background: {
      default: '#EDF2F7',
    },
    text: { primary: '#000000d1' },
  },
});

export default theme;